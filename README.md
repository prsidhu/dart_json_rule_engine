# Game 01

Build a simple engine that can run basic comparisons and return associated events.

rule: {
  "key": "version",
  "operator": "equals",
  "value": "20"
}

fact: {
  "version": "20"
}

event: {
  "name": "match"
}